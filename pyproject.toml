[project]
name = "chunk-pdf"
version = "0.1.0"
description = "Chunk a PDF by its ToC"
requires-python = ">=3.8"
dependencies = [
    "pymupdf>=1.22.3",
]

[project.optional-dependencies]
gui = ["pyside6>=6.4.0"]

[dependency-groups]
dev = ["pytest>=7"]

[tool.uv]
package = false
